# Copy to .env and fill in. agent.ts also looks one level up (Lab_2.6/.env),
# so a single key at the lab root works for every project in the lab.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to claude-sonnet-5.
# CLAUDE_MODEL=claude-opus-5
